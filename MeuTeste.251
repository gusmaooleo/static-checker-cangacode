program TestSuite

declarations
varType integer: shortVar, veryLongVariableNameExceedingThirtyTwoCharactersWhichWillBeTruncated;
varType real[]: dataArray[5], anotherArray[100];
varType string: message;
varType boolean: flag;
varType character: letter;
endDeclarations

/*
 * Comentário externo
 * /* Comentário interno aninhado */
 * continua comentário externo
 */

functions
funcType void: helper(paramType integer: x) {
    // imprime o parâmetro e sai
    print x
    return
} endFunction;

funcType void: main(paramType ?) {
    // atribuições simples
    shortVar := 42
    shortVar := 123456789012345678901234567890123   // inteiro >32 dígitos
    shortVar := 123456789012345678901234567890123.456 // decimal truncado antes do ponto
    shortVar := 1E+10  // notação exponencial
    shortVar := 1E+10 + 50  // notação exponencial
    flag := true
    letter := 'Z'
    message := "Texto com /*não é comentário*/ e //não é linha" 

    if (flag == true) {
        print 0
    } else {
        break
    } endIf

    while (shortVar < 10) {
        shortVar := shortVar + 1
    } endWhile

    break   // pausa a execução
    return  // finaliza sem valor
} endFunction;

endFunctions

endProgram
